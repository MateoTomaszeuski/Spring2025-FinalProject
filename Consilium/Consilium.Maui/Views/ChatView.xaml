<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Consilium.Maui.Views.ChatView"
             xmlns:tools="clr-namespace:Consilium.Maui.Controls"
             Title="ChatView">
    <VerticalStackLayout>
        <CollectionView ItemsSource="{Binding Conversations}" IsVisible="{Binding ShowConversations}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Label Text="{Binding .}"/>
                        <Button Text="Select"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SelectConversationCommand}"
                                CommandParameter="{Binding .}"/>

                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        
        <VerticalStackLayout IsVisible="{Binding ShowChat}">
            <tools:MessagesView ConversationWith="{Binding SelectedConversation}"/>
        </VerticalStackLayout>
    </VerticalStackLayout>
</ContentPage>